{
  "name": "open-arc-extension",
  "version": "0.1.0",
  "private": true,
  "//": "@types/chrome is compile-time typing for the MV3 chrome.* APIs only; it never ships in dist, so it stays a devDependency.",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit && svelte-check --tsconfig ./tsconfig.json"
  },
  "dependencies": {
    "nanoid": "^5.1.5",
    "svelte": "^5.38.1"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.1.3",
    "@types/chrome": "^0.0.280",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  },
  "license": "GPL-3.0-only"
}
